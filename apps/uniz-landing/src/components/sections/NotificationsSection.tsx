export const NotificationsSection = () => {
    return (
        <section id="notifications" className="w-full py-20 bg-white min-h-[400px]">
            {/* Empty section waiting for premium redesign */}
        </section>
    );
};
